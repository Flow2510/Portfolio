export default function Description({ titleDescription, description}) {
    return(
        <section>
            <h1>{titleDescription}</h1>
            <p>{description}</p>
        </section>
    )
}